import './eventPriceContainer.scss';

const EventPriceContainer = (props: any): JSX.Element => {

    return (
        <div className="eventPriceContainer">
            Event Price Container
        </div>
    );
}

export default EventPriceContainer;